const CANVAS_SIZE = [750, 400]

const PHANTOM_START = [
  [14, 11],
  [14, 12]
]

const RAT_START = [14, 3]

const SCALE = 25

const SPEED = 125

const DIRECTIONS = {
  38: [0, -1], // up
  40: [0, 1], // down
  37: [-1, 0], // left
  39: [1, 0] // right
}

export {
  CANVAS_SIZE,
  PHANTOM_START,
  RAT_START,
  SCALE,
  SPEED,
  DIRECTIONS
}
