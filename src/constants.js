const CANVAS_SIZE = [600, 600]

const PHANTOM_START = [
  [8, 10],
  [8, 11]
]

const RAT_START = [8, 5]

const SCALE = 40

const SPEED = 100

const DIRECTIONS = {
  38: [0, -1], // up
  40: [0, 1], // down
  37: [-1, 0], // left
  39: [1, 0] // right
}

export {
  CANVAS_SIZE,
  PHANTOM_START,
  RAT_START,
  SCALE,
  SPEED,
  DIRECTIONS
}
